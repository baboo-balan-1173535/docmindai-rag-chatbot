# DocMind AI v2.0 — Requirements
# Install with: pip install -r requirements.txt

# ── Core framework ────────────────────────────────────────────
flask==3.1.3
python-dotenv==1.2.2
Werkzeug==3.1.6

# ── AI / LLM ─────────────────────────────────────────────────
anthropic==0.86.0

# ── Embeddings + document loaders ────────────────────────────
sentence-transformers==5.3.0
torch==2.10.0
transformers==4.44.2
tokenizers==0.19.1
langchain==1.2.12
langchain-community==0.4.1
langchain-text-splitters==1.1.1
langchain-core==1.2.20

# ── Document parsing ──────────────────────────────────────────
pypdf==6.9.1
docx2txt==0.9

# ── PostgreSQL ────────────────────────────────────────────────
# psycopg2-binary: PostgreSQL driver (binary wheel — no build tools needed)
psycopg2-binary==2.9.9

# ── Vector store ──────────────────────────────────────────────
# FAISS saves the index to faiss_index/ folder on disk — persists across restarts.
# pgvector==0.3.6 is listed here for future migration (requires PG extension installed).
faiss-cpu==1.13.2

# ── Utilities ─────────────────────────────────────────────────
numpy==2.4.3
requests==2.32.5
scikit-learn==1.8.0

# ── Removed from v1 ──────────────────────────────────────────
# faiss-cpu  → replaced by pgvector (PostgreSQL-native vector store)
